import type { Config } from 'tailwindcss'
// Tailwind v4: configuration is done via @theme in globals.css
const config: Config = {
  content: ['./app/**/*.{js,ts,jsx,tsx,mdx}'],
}
export default config
